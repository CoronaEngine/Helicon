#pragma once

#include <ktm/ktm.h>
#include "VariateProxy.h"

namespace EmbeddedShader
{
	template<typename T>
	T clamp(T variable, T a, T b)
	{
	}

	template<typename T>
	T sqrt(T variable)
	{
	}

	template<typename T>
	T pow(T variable, T power)
	{
	}
}
